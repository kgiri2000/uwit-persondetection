# Portal login password protection
PORTAL_PASSWORD=UWIT2026

# Camera capture resolution sent from host to YOLO engine (increase/decrease to change resolution)
DETECTION_WIDTH=400
DETECTION_HEIGHT=300

# YOLOv8 inference size (smaller is faster on CPU: e.g. 320 or 256. Use 640 for max precision)
YOLO_IMGSZ=320
